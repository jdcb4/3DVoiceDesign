{
  "name": "voicedesign3d-viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build && node scripts/viewer-notices.mjs",
    "test:ui": "playwright test"
  },
  "dependencies": {
    "three": "^0.180.0",
    "lucide": "^0.468.0"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0",
    "@playwright/test": "^1.55.0"
  }
}
